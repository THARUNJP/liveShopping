export enum SessionCallType {
  RTC = "RTC",
  SFU = "SFU",
}

// Participant roles
export enum SessionRole {
  HOST = "HOST",
  USER = "USER",
}

// Session status
export enum SessionStatus {
  ACTIVE = "ACTIVE",
  ENDED = "ENDED",
}